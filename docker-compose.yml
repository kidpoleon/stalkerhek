services:
  stalkerhek:
    build: .
    container_name: stalkerhek
    restart: unless-stopped

    # Recommended for LAN usage so that per-profile ports you choose are reachable.
    # With host networking, you do NOT need to pre-map every HLS/Proxy port.
    # NOTE: Host networking works on Linux. On Docker Desktop (Windows/macOS), use the bridge-mode example below.
    network_mode: host

    environment:
      - STALKERHEK_PROFILES_FILE=/data/profiles.json
      - STALKERHEK_AUTH_FILE=/data/auth.json
      - STALKERHEK_FILTERS_FILE=/data/filters.json
      - STALKERHEK_ROOT=/app
      # Process exits cleanly every 24h; Docker restarts it (config in /data is kept).
      - STALKERHEK_RESTART_HOURS=24
    volumes:
      - ./data:/data

    # Optional GPU (Linux + Intel/AMD VAAPI). Stalkerhek proxies streams; ffmpeg is used for
    # diagnostics and future transcode hooks. Uncomment if your host exposes /dev/dri:
    # devices:
    #   - /dev/dri:/dev/dri

# Bridge-mode example (Docker Desktop / non-Linux):
# You MUST publish every HLS/Proxy port you plan to use.
#
# services:
#   stalkerhek:
#     build: .
#     container_name: stalkerhek
#     restart: unless-stopped
#     ports:
#       - "4400:4400"  # WebUI
#       - "6600:6600"  # HLS (example)
#       - "6800:6800"  # Proxy (example)
#     environment:
#       - STALKERHEK_PROFILES_FILE=/data/profiles.json
#       - STALKERHEK_AUTH_FILE=/data/auth.json
#       - STALKERHEK_FILTERS_FILE=/data/filters.json
#       - STALKERHEK_ROOT=/app
#       - STALKERHEK_RESTART_HOURS=24
#     volumes:
#       - ./data:/data
